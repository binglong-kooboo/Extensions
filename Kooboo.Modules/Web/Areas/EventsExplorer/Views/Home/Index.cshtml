@using Kooboo.Modules.EventsExplorer.Models

@model PagedList<WebEventModel>
@{
    ViewBag.Title = "Index";
}

<div class="topbar clearfix">
    <h1 class="title">
        @ViewBag.Title
    </h1>
</div>

@Html.GridFor2(typeof(WebEventModel), Model)
<div class="pagination right">
    @Html.Pager(Model, "", ViewContext.RequestContext.AllRouteValues(), null)
</div>